import { MaterialManifestModule } from './material-manifest.module';

describe('MaterialManifestModule', () => {
  it('should work', () => {
    expect(new MaterialManifestModule()).toBeDefined();
  });
});
